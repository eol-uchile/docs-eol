pull_request_rules:
  - name: Continuous integration with Bors-like strategy
    conditions:
      - status-success=docker-build-success
      - base=master
    actions:
      merge:
        method: merge
