pull_request_rules:
  - name: Continuous integration with Bors-like strategy
    conditions:
      - check-success=build
      - base=main
    actions:
      merge:
        method: merge
        commit_message_template: "Merged PR: {{ title }} (#{{ number }})"